# 开发平台设计文档 - 数据库设计

## 1. 设计原则

- **命名规范**：
  - 表名：`snake_case`，如 `sys_application`、`dev_model_field`；建议统一前缀 `dev_` 表示开发平台资产表。
  - 字段名：`snake_case`。
  - 索引名：`idx_{table}_{field}`；唯一索引：`uk_{table}_{field}`。
  - 外键约束：本期不强制使用物理外键（考虑后期分库分表与性能），采用逻辑外键 + 应用层校验 + 必要索引。
- **主键**：所有核心业务表主键 `id` 使用 `BIGINT`（雪花 ID）。
- **软删除**：`is_deleted TINYINT DEFAULT 0`（0=未删除，1=已删除）。
- **审计字段**：`create_time DATETIME`、`update_time DATETIME`、`create_by BIGINT`、`update_by BIGINT`。
- **编码**：`CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci`。
- **存储引擎**：`InnoDB`。
- **状态枚举**：启用停用统一 `status TINYINT`（1=启用，0=停用）。

## 2. ER 关系总览

```
┌─────────────────────┐
│  dev_application    │  应用（根）
│  PK id              │
│  UK code            │
└─────────┬───────────┘
          │ 1:N
┌─────────▼───────────┐          ┌─────────────────────┐
│  dev_microservice   │ 1:N ────▶│  dev_model          │  模型
│  PK id              │          │  PK id               │
│  FK application_id  │          │  FK microservice_id  │
│  UK(app_id, code)   │          │  UK(ms_id, code)     │
└─────────┬───────────┘          └─────────┬───────────┘
          │ 1:N                             │ 1:N
          │  (模型字段)         ┌──────────▼──────────┐
          │                     │  dev_model_field    │  字段
          │                     │  PK id               │
          │                     │  FK model_id         │
          │                     │  → metadata_id(FK)   │
          │                     └─────────────────────┘
          │ 1:N
┌─────────▼───────────┐
│  dev_service        │  服务定义（接口）
│  PK id              │
│  FK microservice_id │
│  UK(ms_id, code)    │
│  UK(ms_id, path)    │
└─────────┬───────────┘
          │ 1:N  (入参/出参)
┌─────────▼───────────┐
│  dev_service_param  │  入参/出参
│  PK id              │
│  FK service_id      │
│  → field_id(FK)     │  可选，关联模型字段
└─────────────────────┘

┌─────────────────────┐
│  dev_metadata       │  元数据（枚举/字典）
│  PK id              │
│  FK application_id  │
│  UK(app_id, code)   │
└─────────┬───────────┘
          │ 1:N
┌─────────▼───────────┐
│  dev_metadata_item  │  元数据项
│  PK id              │
│  FK metadata_id     │
│  UK(mid, item_code) │
└─────────────────────┘

┌─────────────────────┐          ┌─────────────────────┐
│ dev_orchestration   │ 1:N ────▶│ dev_orch_node       │  节点
│ PK id               │          │ PK id                │
│ FK microservice_id  │          │ FK orchestration_id  │
│ UK(ms_id, code)     │          │  → service_id(FK)    │
└─────────┬───────────┘          └─────────┬───────────┘
          │ 1:N (连线)                     │
          │                    ┌──────────▼──────────┐
          │                    │ dev_orch_edge       │  连线
          │                    │ PK id                │
          └───────────────────▶│ FK orchestration_id │
                               │ → from_node_id       │
                               │ → to_node_id         │
                               └─────────────────────┘
```

## 3. 表结构明细

### 3.1 应用表 `dev_application`

| 字段名 | 类型 | 约束 | 默认 | 说明 |
| --- | --- | --- | --- | --- |
| id | BIGINT | PK | | 主键（雪花 ID） |
| name | VARCHAR(128) | NOT NULL | | 应用名称 |
| code | VARCHAR(64) | NOT NULL, UK | | 应用编码，全局唯一 |
| description | VARCHAR(512) | | NULL | 应用描述 |
| version | VARCHAR(32) | | '1.0.0' | 版本 |
| status | TINYINT | NOT NULL | 1 | 状态（1=启用，0=停用） |
| is_deleted | TINYINT | NOT NULL | 0 | 软删除 |
| create_time | DATETIME | NOT NULL | CURRENT_TIMESTAMP | 创建时间 |
| update_time | DATETIME | NOT NULL | CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP | 更新时间 |
| create_by | BIGINT | | NULL | 创建人 |
| update_by | BIGINT | | NULL | 更新人 |

**索引：**
- `uk_dev_application_code` UNIQUE (`code`, `is_deleted`)
- `idx_dev_application_status` (`status`)

---

### 3.2 微服务表 `dev_microservice`

| 字段名 | 类型 | 约束 | 默认 | 说明 |
| --- | --- | --- | --- | --- |
| id | BIGINT | PK | | 主键 |
| application_id | BIGINT | NOT NULL, IDX | | 所属应用 ID |
| name | VARCHAR(128) | NOT NULL | | 微服务名称 |
| code | VARCHAR(64) | NOT NULL | | 微服务编码（应用内唯一） |
| description | VARCHAR(512) | | NULL | 描述 |
| version | VARCHAR(32) | | '1.0.0' | 版本 |
| status | TINYINT | NOT NULL | 1 | 状态 |
| is_deleted | TINYINT | NOT NULL | 0 | 软删除 |
| create_time / update_time / create_by / update_by | 同上 | | | 审计字段 |

**索引：**
- `uk_dev_ms_app_code` UNIQUE (`application_id`, `code`, `is_deleted`)
- `idx_dev_ms_application_id` (`application_id`)

---

### 3.3 模型表 `dev_model`

| 字段名 | 类型 | 约束 | 默认 | 说明 |
| --- | --- | --- | --- | --- |
| id | BIGINT | PK | | 主键 |
| microservice_id | BIGINT | NOT NULL, IDX | | 所属微服务 ID |
| name | VARCHAR(128) | NOT NULL | | 模型名称 |
| code | VARCHAR(64) | NOT NULL | | 模型编码（微服务内唯一） |
| description | VARCHAR(512) | | NULL | 描述 |
| is_deleted | TINYINT | NOT NULL | 0 | 软删除 |
| create_time / update_time / create_by / update_by | 同上 | | | 审计字段 |

**索引：**
- `uk_dev_model_ms_code` UNIQUE (`microservice_id`, `code`, `is_deleted`)
- `idx_dev_model_microservice_id` (`microservice_id`)

---

### 3.4 模型字段表 `dev_model_field`

| 字段名 | 类型 | 约束 | 默认 | 说明 |
| --- | --- | --- | --- | --- |
| id | BIGINT | PK | | 主键 |
| model_id | BIGINT | NOT NULL, IDX | | 所属模型 ID |
| name | VARCHAR(64) | NOT NULL | | 字段名（系统标识） |
| display_name | VARCHAR(128) | NOT NULL | | 显示名 |
| field_type | VARCHAR(32) | NOT NULL | | 数据类型：TEXT/INTEGER/DECIMAL/DATE/DATETIME/BOOLEAN/ENUM |
| length | INT | | NULL | 长度（文本/数字适用） |
| precision | INT | | NULL | 精度（小数适用） |
| is_required | TINYINT | NOT NULL | 0 | 是否必填（1=是，0=否） |
| is_primary | TINYINT | NOT NULL | 0 | 是否主键 |
| is_unique | TINYINT | NOT NULL | 0 | 是否唯一 |
| is_index | TINYINT | NOT NULL | 0 | 是否索引 |
| default_value | VARCHAR(256) | | NULL | 默认值 |
| metadata_id | BIGINT | | NULL | 关联元数据 ID（枚举类型有效） |
| sort_order | INT | NOT NULL | 0 | 排序 |
| field_comment | VARCHAR(512) | | NULL | 字段说明 |
| is_deleted | TINYINT | NOT NULL | 0 | 软删除 |
| create_time / update_time / create_by / update_by | 同上 | | | 审计字段 |

**索引：**
- `uk_dev_mf_model_name` UNIQUE (`model_id`, `name`, `is_deleted`)
- `idx_dev_mf_model_id` (`model_id`)
- `idx_dev_mf_metadata_id` (`metadata_id`)

---

### 3.5 元数据表 `dev_metadata`

| 字段名 | 类型 | 约束 | 默认 | 说明 |
| --- | --- | --- | --- | --- |
| id | BIGINT | PK | | 主键 |
| application_id | BIGINT | NOT NULL, IDX | | 所属应用 ID |
| name | VARCHAR(128) | NOT NULL | | 元数据名称 |
| code | VARCHAR(64) | NOT NULL | | 元数据编码（应用内唯一） |
| description | VARCHAR(512) | | NULL | 描述 |
| status | TINYINT | NOT NULL | 1 | 状态 |
| is_deleted | TINYINT | NOT NULL | 0 | 软删除 |
| create_time / update_time / create_by / update_by | 同上 | | | 审计字段 |

**索引：**
- `uk_dev_meta_app_code` UNIQUE (`application_id`, `code`, `is_deleted`)
- `idx_dev_meta_application_id` (`application_id`)

---

### 3.6 元数据项表 `dev_metadata_item`

| 字段名 | 类型 | 约束 | 默认 | 说明 |
| --- | --- | --- | --- | --- |
| id | BIGINT | PK | | 主键 |
| metadata_id | BIGINT | NOT NULL, IDX | | 所属元数据 ID |
| item_code | VARCHAR(64) | NOT NULL | | 元数据项编码（元数据内唯一） |
| item_name | VARCHAR(128) | NOT NULL | | 元数据项名称 |
| item_value | VARCHAR(256) | | NULL | 元数据项值 |
| sort_order | INT | NOT NULL | 0 | 排序 |
| status | TINYINT | NOT NULL | 1 | 状态 |
| is_deleted | TINYINT | NOT NULL | 0 | 软删除 |
| create_time / update_time / create_by / update_by | 同上 | | | 审计字段 |

**索引：**
- `uk_dev_mi_meta_code` UNIQUE (`metadata_id`, `item_code`, `is_deleted`)
- `idx_dev_mi_metadata_id` (`metadata_id`)

---

### 3.7 服务定义表 `dev_service`

> 表名使用 `dev_service`（Java 实体命名为 `ServiceDef` 以避免与 Spring `@Service` 冲突）。

| 字段名 | 类型 | 约束 | 默认 | 说明 |
| --- | --- | --- | --- | --- |
| id | BIGINT | PK | | 主键 |
| microservice_id | BIGINT | NOT NULL, IDX | | 所属微服务 ID |
| name | VARCHAR(128) | NOT NULL | | 服务名称 |
| code | VARCHAR(64) | NOT NULL | | 服务编码（微服务内唯一） |
| description | VARCHAR(1024) | | NULL | 功能说明 |
| http_method | VARCHAR(16) | NOT NULL | 'POST' | 请求方式：GET/POST/PUT/DELETE |
| service_path | VARCHAR(256) | NOT NULL | | 服务路径 |
| category | VARCHAR(64) | | NULL | 分组/分类 |
| status | TINYINT | NOT NULL | 1 | 状态 |
| is_deleted | TINYINT | NOT NULL | 0 | 软删除 |
| create_time / update_time / create_by / update_by | 同上 | | | 审计字段 |

**索引：**
- `uk_dev_svc_ms_code` UNIQUE (`microservice_id`, `code`, `is_deleted`)
- `uk_dev_svc_ms_path` UNIQUE (`microservice_id`, `service_path`, `is_deleted`)
- `idx_dev_svc_microservice_id` (`microservice_id`)

---

### 3.8 服务参数表 `dev_service_param`

（入参与出参统一存本表，通过 `param_type` 区分）

| 字段名 | 类型 | 约束 | 默认 | 说明 |
| --- | --- | --- | --- | --- |
| id | BIGINT | PK | | 主键 |
| service_id | BIGINT | NOT NULL, IDX | | 所属服务 ID |
| param_type | TINYINT | NOT NULL | 1 | 参数类型：1=入参(INPUT)，2=出参(OUTPUT) |
| param_name | VARCHAR(64) | NOT NULL | | 参数名 |
| data_type | VARCHAR(32) | NOT NULL | | 数据类型：基础类型/模型关联 |
| is_required | TINYINT | NOT NULL | 1 | 是否必填（出参可忽略） |
| default_value | VARCHAR(256) | | NULL | 默认值 |
| model_field_id | BIGINT | | NULL | 关联模型字段 ID（可选） |
| sort_order | INT | NOT NULL | 0 | 排序 |
| param_comment | VARCHAR(512) | | NULL | 参数说明 |
| is_deleted | TINYINT | NOT NULL | 0 | 软删除 |
| create_time / update_time / create_by / update_by | 同上 | | | 审计字段 |

**索引：**
- `uk_dev_sp_svc_type_name` UNIQUE (`service_id`, `param_type`, `param_name`, `is_deleted`)
- `idx_dev_sp_service_id` (`service_id`)
- `idx_dev_sp_field_id` (`model_field_id`)

---

### 3.9 服务编排表 `dev_orchestration`

| 字段名 | 类型 | 约束 | 默认 | 说明 |
| --- | --- | --- | --- | --- |
| id | BIGINT | PK | | 主键 |
| microservice_id | BIGINT | NOT NULL, IDX | | 所属微服务 ID |
| name | VARCHAR(128) | NOT NULL | | 编排名称 |
| code | VARCHAR(64) | NOT NULL | | 编排编码（微服务内唯一） |
| description | VARCHAR(1024) | | NULL | 描述 |
| status | TINYINT | NOT NULL | 1 | 状态 |
| is_deleted | TINYINT | NOT NULL | 0 | 软删除 |
| create_time / update_time / create_by / update_by | 同上 | | | 审计字段 |

**索引：**
- `uk_dev_orch_ms_code` UNIQUE (`microservice_id`, `code`, `is_deleted`)
- `idx_dev_orch_microservice_id` (`microservice_id`)

---

### 3.10 编排节点表 `dev_orch_node`

| 字段名 | 类型 | 约束 | 默认 | 说明 |
| --- | --- | --- | --- | --- |
| id | BIGINT | PK | | 主键 |
| orchestration_id | BIGINT | NOT NULL, IDX | | 所属编排 ID |
| node_key | VARCHAR(64) | NOT NULL | | 前端画布节点唯一标识（由前端生成） |
| node_type | VARCHAR(32) | NOT NULL | | 节点类型：START / SERVICE / CONDITION / LOOP / END |
| node_name | VARCHAR(128) | | NULL | 节点显示名称 |
| service_id | BIGINT | | NULL | 被调用服务 ID（SERVICE 节点有效） |
| config_json | LONGTEXT | | NULL | 节点配置 JSON（条件表达式、入参映射、循环条件等） |
| x_pos | INT | | NULL | 画布 X 坐标 |
| y_pos | INT | | NULL | 画布 Y 坐标 |
| sort_order | INT | NOT NULL | 0 | 排序（若使用有序结构） |
| is_deleted | TINYINT | NOT NULL | 0 | 软删除 |
| create_time / update_time | 同上 | | | 审计字段 |

**索引：**
- `uk_dev_on_orch_key` UNIQUE (`orchestration_id`, `node_key`, `is_deleted`)
- `idx_dev_on_orch_id` (`orchestration_id`)
- `idx_dev_on_service_id` (`service_id`)

---

### 3.11 编排连线表 `dev_orch_edge`

| 字段名 | 类型 | 约束 | 默认 | 说明 |
| --- | --- | --- | --- | --- |
| id | BIGINT | PK | | 主键 |
| orchestration_id | BIGINT | NOT NULL, IDX | | 所属编排 ID |
| edge_key | VARCHAR(64) | NOT NULL | | 连线唯一标识 |
| from_node_key | VARCHAR(64) | NOT NULL | | 起点 node_key |
| to_node_key | VARCHAR(64) | NOT NULL | | 终点 node_key |
| condition_expr | VARCHAR(1024) | | NULL | 条件表达式（条件分支连线用） |
| label_text | VARCHAR(256) | | NULL | 连线上展示文字 |
| is_deleted | TINYINT | NOT NULL | 0 | 软删除 |
| create_time / update_time | 同上 | | | 审计字段 |

**索引：**
- `uk_dev_oe_orch_edgekey` UNIQUE (`orchestration_id`, `edge_key`, `is_deleted`)
- `idx_dev_oe_orch_id` (`orchestration_id`)

## 4. 关键数据字典

### 4.1 状态字段 `status`

| 值 | 含义 |
| --- | --- |
| 0 | 停用 |
| 1 | 启用 |

### 4.2 字段数据类型 `field_type`（模型字段）

| 值 | 含义 |
| --- | --- |
| TEXT | 短文本 |
| LONGTEXT | 长文本 |
| INTEGER | 整数 |
| DECIMAL | 小数 |
| DATE | 日期 |
| DATETIME | 日期时间 |
| BOOLEAN | 布尔 |
| ENUM | 枚举（需 `metadata_id`） |

### 4.3 参数类型 `param_type`（服务参数）

| 值 | 含义 |
| --- | --- |
| 1 | 入参（INPUT） |
| 2 | 出参（OUTPUT） |

### 4.4 节点类型 `node_type`（编排节点）

| 值 | 含义 |
| --- | --- |
| START | 开始节点 |
| SERVICE | 服务调用节点 |
| CONDITION | 条件判断节点 |
| LOOP | 循环节点 |
| END | 结束节点 |

## 5. 数据库初始化脚本（DDL 示例）

> 生产项目应以 Flyway/Liquibase 管理版本迁移。以下为核心建表示例，完整 DDL 需包含所有表。

```sql
CREATE TABLE `dev_application` (
  `id` BIGINT NOT NULL COMMENT '主键',
  `name` VARCHAR(128) NOT NULL COMMENT '应用名称',
  `code` VARCHAR(64) NOT NULL COMMENT '应用编码',
  `description` VARCHAR(512) DEFAULT NULL COMMENT '描述',
  `version` VARCHAR(32) DEFAULT '1.0.0' COMMENT '版本',
  `status` TINYINT NOT NULL DEFAULT 1 COMMENT '状态 1启用 0停用',
  `is_deleted` TINYINT NOT NULL DEFAULT 0 COMMENT '软删除',
  `create_time` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
  `update_time` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  `create_by` BIGINT DEFAULT NULL,
  `update_by` BIGINT DEFAULT NULL,
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_dev_application_code` (`code`, `is_deleted`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='应用表';

CREATE TABLE `dev_microservice` (
  `id` BIGINT NOT NULL,
  `application_id` BIGINT NOT NULL COMMENT '所属应用',
  `name` VARCHAR(128) NOT NULL,
  `code` VARCHAR(64) NOT NULL,
  `description` VARCHAR(512) DEFAULT NULL,
  `version` VARCHAR(32) DEFAULT '1.0.0',
  `status` TINYINT NOT NULL DEFAULT 1,
  `is_deleted` TINYINT NOT NULL DEFAULT 0,
  `create_time` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
  `update_time` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_dev_ms_app_code` (`application_id`, `code`, `is_deleted`),
  KEY `idx_dev_ms_application_id` (`application_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='微服务表';

-- 其余 9 张表按 3.3~3.11 结构定义，索引按上述规范创建。
```
